Adds download functionality to vk.com music pages and nothing else. It rotates play button which turns it into a download button.

Unlike some existing extensions with this functionality, this one *only* adds the dl feature thus also
requires less permissions.

`npm run build`


todo: bookmarklet version
